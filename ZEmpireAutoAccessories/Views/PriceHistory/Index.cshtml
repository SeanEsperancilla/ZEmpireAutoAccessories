@model IEnumerable<ZEmpireAutoAccessories.Models.PriceHistory>

@{
    ViewData["Title"] = "Price History";
}

<style>
    .zx-page-head h1 {
        font-family: 'Oswald',system-ui,sans-serif;
        font-weight: 700;
        font-size: 22px;
        margin: 0 0 18px;
        color: #0b0b0c;
    }

    .zx-card {
        background: #fff;
        border: 1px solid #e8ebf0;
        border-radius: 14px;
        overflow: hidden;
    }

    .zx-table thead {
        background: #0b0b0c;
        color: #fff;
    }

        .zx-table thead th {
            border-bottom: none;
            font-weight: 600;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: .3px;
        }

    .zx-table td, .zx-table th {
        vertical-align: middle;
    }

    .up {
        color: #b91c1c;
        font-weight: 600;
    }

    .down {
        color: #15803d;
        font-weight: 600;
    }
</style>

<div class="zx-page-head">
    <h1>Price History</h1>
</div>

<div class="zx-card">
    <table class="table zx-table mb-0">
        <thead>
            <tr>
                <th>Date</th>
                <th>Product</th>
                <th>Classification</th>
                <th>Panel</th>
                <th>Old Price</th>
                <th>New Price</th>
                <th>Changed By</th>
                <th class="text-end pe-3">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var h in Model)
            {
                <tr>
                    <td>@h.DateChanged.ToString("MMM d, yyyy h:mm tt")</td>
                    <td>@h.Pricing.Product.ProductName</td>
                    <td>@h.Pricing.VehicleClassification.ClassificationName</td>
                    <td>@h.Pricing.Panel.PanelName</td>
                    <td>@("₱" + h.OldPrice.ToString("N2"))</td>
                    <td class="@(h.NewPrice > h.OldPrice ? "up" : "down")">@("₱" + h.NewPrice.ToString("N2"))</td>
                    <td>@h.User.UserName</td>
                    <td class="text-end pe-3">
                        <a asp-controller="Pricing" asp-action="Details" asp-route-id="@h.PricingID">View Pricing</a>
                    </td>
                </tr>
            }
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="8" class="text-center text-muted py-4">No price changes recorded yet.</td>
                </tr>
            }
        </tbody>
    </table>
</div>