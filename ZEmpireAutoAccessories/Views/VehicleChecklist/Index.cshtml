@model IEnumerable<ZEmpireAutoAccessories.Models.VehicleChecklist>

@{
    ViewData["Title"] = "Vehicle Checklists";
}

<style>
    .zx-page-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 18px;
    }

        .zx-page-head h1 {
            font-family: 'Oswald',system-ui,sans-serif;
            font-weight: 700;
            font-size: 22px;
            margin: 0;
            color: #0b0b0c;
        }

    .btn-zx {
        background: var(--zx-red);
        border-color: var(--zx-red);
        color: #fff;
        font-weight: 600;
    }

        .btn-zx:hover {
            background: var(--zx-red2);
            border-color: var(--zx-red2);
            color: #fff;
        }

    .zx-card {
        background: #fff;
        border: 1px solid #e8ebf0;
        border-radius: 14px;
        overflow: hidden;
    }

    .zx-table thead {
        background: #0b0b0c;
        color: #fff;
    }

        .zx-table thead th {
            border-bottom: none;
            font-weight: 600;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: .3px;
        }

    .zx-table td, .zx-table th {
        vertical-align: middle;
    }

    .zx-actions a {
        margin-right: 10px;
        font-weight: 600;
        font-size: 13.5px;
    }
</style>

<div class="zx-page-head">
    <h1>Vehicle Checklists</h1>
    <a class="btn btn-zx" asp-action="Create">+ New Checklist</a>
</div>

<div class="zx-card">
    <table class="table zx-table mb-0">
        <thead>
            <tr>
                <th>Job Order</th>
                <th>Customer</th>
                <th>Vehicle</th>
                <th>Date</th>
                <th>Recorded By</th>
                <th class="text-end pe-3">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.JobOrder.JobOrderNumber</td>
                    <td>@item.JobOrder.Customer.FullName</td>
                    <td>@(item.JobOrder.Vehicle.PlateNumber ?? "—")</td>
                    <td>@item.ChecklistDate.ToString("MMM d, yyyy")</td>
                    <td>@item.User.UserName</td>
                    <td class="text-end pe-3 zx-actions">
                        <a asp-action="Details" asp-route-id="@item.ChecklistID">View</a>
                        <a asp-action="Delete" asp-route-id="@item.ChecklistID" class="text-danger">Delete</a>
                    </td>
                </tr>
            }
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="6" class="text-center text-muted py-4">No checklists yet.</td>
                </tr>
            }
        </tbody>
    </table>
</div>